# Web UI Read Models

`read_models.json` defines the minimum projections required by the first V2 product shell.

The file is intentionally implementation-neutral so later React code can consume the same contract without forcing a frontend toolchain into Phase A.
