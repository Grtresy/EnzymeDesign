# Deprecated Fixture Location

Committed HPC tool input samples now live under:

`apps/mcp-hpc-runner/fixtures/hpc_tool_samples/`

This directory is retained only to make the old location explicit during the
transition. New tests, docs, and contract builders should use
`hpc_tool_samples/`.
