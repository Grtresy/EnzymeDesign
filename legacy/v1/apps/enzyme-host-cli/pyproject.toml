[project]
name = "enzyme-host-cli"
version = "0.1.0"
description = "Minimal host CLI/runtime for OpenZyme project workflows"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "enzyme-host-runtime",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.2.0",
]

[project.scripts]
enzyme = "enzyme_host_cli.cli:main"

[tool.uv.sources]
enzyme-host-runtime = { workspace = true }

[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/enzyme_host_cli"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
