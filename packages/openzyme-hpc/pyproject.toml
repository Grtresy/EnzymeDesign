[project]
name = "openzyme-hpc"
version = "0.1.0"
description = "HPC target、inventory 与远端 workspace 语义插件"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "openzyme-contracts",
  "openzyme-extension-spi",
  "openzyme-execution-contracts",
]

[project.entry-points."openzyme.extensions"]
"openzyme.hpc" = "openzyme_hpc.manifest_locator:locate_component_manifest"

[project.optional-dependencies]
dev = [
  "pytest>=9.0.0",
]

[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/openzyme_hpc"]

[tool.openzyme.component]
component_id = "openzyme.hpc"
component_kind = "plugin"
migration_state = "target_implemented_not_cutover"
current_composition_owner = "uncomposed"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
