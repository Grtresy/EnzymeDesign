[project]
name = "openzyme-compute"
version = "0.1.0"
description = "修订绑定的正式计算生命周期插件"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "openzyme-contracts",
  "openzyme-extension-spi",
  "openzyme-execution-contracts",
]

[project.entry-points."openzyme.extensions"]
"openzyme.compute" = "openzyme_compute.manifest_locator:locate_component_manifest"

[project.optional-dependencies]
dev = [
  "pytest>=9.0.0",
]

[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/openzyme_compute"]

[tool.openzyme.component]
component_id = "openzyme.compute"
component_kind = "plugin"
migration_state = "target_implemented_not_cutover"
current_composition_owner = "uncomposed"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
