[project]
name = "enzymedesign-vina"
version = "0.1.0"
description = "Vina typed docking tool 与 Driver 插件"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "enzymedesign-core",
  "openzyme-contracts",
  "openzyme-execution-contracts",
  "openzyme-extension-spi",
]

[project.entry-points."openzyme.extensions"]
"enzymedesign.vina" = "enzymedesign_vina.manifest_locator:locate_component_manifest"
"enzymedesign.vina.hpc" = "enzymedesign_vina.manifest_locator:locate_hpc_driver_manifest"
"enzymedesign.vina.local" = "enzymedesign_vina.manifest_locator:locate_local_driver_manifest"

[project.optional-dependencies]
dev = [
  "pytest>=9.0.0",
]

[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/enzymedesign_vina"]

[tool.openzyme.component]
component_id = "enzymedesign.vina"
component_kind = "product_plugin"
migration_state = "target_implemented_not_cutover"
current_composition_owner = "uncomposed"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
