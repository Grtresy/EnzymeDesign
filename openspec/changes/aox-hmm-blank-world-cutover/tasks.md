## 1. Reference and contract baseline

- [x] 1.1 Record the authorized notebook/runner hashes, formula-derived golden rows, corrected integer-tenths boundary, and historical-output caveats without copying reference outputs into live roots.
- [x] 1.2 Implement the dependency-free `aox_motif_rule_score@1` contract, digest identities, aligned FASTA parser, exact coordinate mapping, scoring, and canonical row serialization in `openzyme_pipeline`.
- [x] 1.3 Add minimal immutable golden fixtures and focused tests for exact score/pass/order plus missing, duplicate, truncated, unequal-width, digest-drift, and legacy-schema failures.
- [x] 1.4 Add a versioned real-sequence similarity calculation and parsers that bind graph nodes/edges to actual sequence and CD-HIT identities, including schema-valid empty output.
- [x] 1.5 Replace AOX validators and execution summary fields with the canonical motif contract and reject `activity_score`, `seq_score`, `pass_rule`, constant graph data, and synthetic sequences as cutover evidence.

## 2. Harness workflow-selection friction

- [x] 2.1 Add explicit `workflow_refs` subset selection to `task.delegate`, validate authorization and target-role requirements before claim, and persist the exact manifest snapshots.
- [x] 2.2 Remove implicit parent-focus workflow inheritance so omitted/empty refs mean no binding; return LLM-readable errors for unauthorized, duplicate, drifted, or incompatible refs.
- [x] 2.3 Add harness/protocol tests for executor-only binding, researcher/reporter no-binding, explicit empty selection, role mismatch, manifest drift, and durable replay.
- [x] 2.4 Document the corrected delegation contract in `docs/v3/` and, for every newly found large harness issue, create one detailed `docs/v3/architecture-proposals/` document without implementing that proposal.

## 3. Literature provider quorum

- [x] 3.1 Introduce typed provider outcome/provenance/failure records and a bounded HTTP policy that handles safe request identity, timeouts, retryable status, `Retry-After`, attempts, schema drift, empty results, response digest, and retrieval time.
- [x] 3.2 Update PubMed normalization to preserve PMID, supplied DOI, title, authors, venue/date, source locator, NCBI identity, request/response provenance, and to distinguish empty results from schema drift.
- [x] 3.3 Update Semantic Scholar and Tavily adapters so rate limiting, exhaustion, absence, and empty results persist explicit enrichment degradation without synthetic fallback.
- [x] 3.4 Establish invocation/operation state before provider I/O, terminate it on every outcome, convert typed failures to LLM-readable tool results, and seal licensed/safe provider evidence through the artifact boundary.
- [x] 3.5 Wire Host settings so PubMed/Semantic Scholar remain usable without Tavily and secrets/private URLs never enter errors, evidence, or public projections.
- [x] 3.6 Add tests for 429/`Retry-After`, transient recovery, non-retryable status, empty-vs-schema drift, required-vs-enrichment quorum, terminal invocation, real sealing, repository round-trip, and safe projection.
- [x] 3.7 Preserve bounded iterative PubMed strategy while requiring exactly one primary artifact adoption in researcher `task.finish`, close nullable task/invocation/artifact/source lineage in collector/blocker/offline verifier, and defer complete invocation-history sealing to its own architecture proposal.

## 4. AOX provider and toolchain identity chain

- [x] 4.1 Make one NCBI fetch prove exact requested-to-resolved identity for all 14 inputs (13 HMM references including `9AVH_A` plus `AAB57849.1`), split them through the versioned 13-record model-reference and single-record coordinate-reference contracts, and fail on missing, extra, duplicate, or mismatched records while sealing aggregate and per-sequence digests.
- [x] 4.2 Strictly validate EBI HMMER `refprot` hits, derive the exact score-`>200` UniProt accession artifact before any UniProt call, and preserve request/operation, page, raw/parsed response, numeric field, and digest provenance.
- [x] 4.3 Preserve UniProt reviewed status, release/version, retrieved time, response and sequence digests; express cross-database mappings as annotations and require explicit choice on sequence mismatch.
- [x] 4.4 Migrate the AOX sandbox execution to consume real provider/HPC outputs through `openzyme_pipeline`, build the HMM only from the exact 13 references, assemble `AAB57849.1`-first scoring input from post-UniProt targets, run reached MAFFT/hmmbuild/hmmalign/CD-HIT/scoring/similarity operations, and emit normalized sealed artifacts without constants or copied scores.
- [x] 4.5 Add provider/toolchain tests for exact-14 reference completeness and split, HMMER schema/score-filter/upstream-empty receipt, UniProt identity/length-join conflicts, reached-branch operation omission, tool params/versions, output declarations, graph lineage, and empty-result reporting.

## 5. Workflow, product path, and reporting

- [x] 5.1 Version and digest-pin AOX workflow knowledge for the required accessions, provider quorum, scoring contract, artifact schemas, empty-result semantics, and scientific fail-closed conditions while preserving agent strategy freedom.
- [x] 5.2 Replace the single-executor S15 live scenario with a one-message canonical master path that delegates researcher/executor/reporter work and advances only through public message, runtime drain, approval, workspace, event, and report APIs.
- [x] 5.3 Require explicit task business exits, canonical approval continuity, normalized artifact registration, report claim/source links, `report.publish`, final master response, and research/execution/report participation in cutover validation.
- [x] 5.4 Quarantine deterministic/seeded AOX fixtures as `fixture_non_cutover`, remove historical live-passed claims, and ensure seeded smoke cannot satisfy the cutover validator.
- [x] 5.5 Update Host API projections and Web UI to show safe provider/quorum/operation/artifact/report/evidence status and preserve the same operation identity across approval resume.
- [x] 5.6 Add API/eval/UI tests for required/degraded evidence, approval continuity, published report, fixture rejection, empty scientific result, and projection secrecy.

## 6. Blank-world evidence and campaign

- [x] 6.1 Implement blank-world preflight that creates and proves unique empty SQLite/blob/artifact/sandbox/HPC roots, records allowed immutable prerequisites, bypasses evidence-bearing caches, and rejects preloaded science.
- [x] 6.2 Implement canonical attempt bundles covering commit/config/workflow/scoring/image/SDK/provider/toolchain, clean roots, approvals, operations, tasks, artifacts, report, final answer, warnings, degradation, and outcome.
- [x] 6.3 Implement a network-free verifier that recomputes canonical bundle/artifact/scoring/lineage/report digests and reports the exact missing or mismatched identity.
- [x] 6.4 Add tamper tests for artifact bytes, provenance, operation, report, and bundle fields plus secret/path leakage tests.
- [x] 6.5 Implement and verify `aox_known_positive_probe@2` with the fixed two-NCBI/two-UniProt globin identities and exact six provider/HPC operations, raw-response digests, isolated task/workspace/sandbox/source/artifact identities, and no probe flow into formal AOX results; distinguish healthy empty result from discovery.
- [x] 6.6 Implement a campaign driver that enforces two independent positive attempts on one commit/config identity followed by one controlled required-chain fault attempt and derives GO only from all three verified digests.
- [x] 6.7 Integrate the persistent MICU 500M ledger without reset, migrate the exact legacy 100M fixed policy while preserving all prior usage, keep caller-selected lower limits durable, and record pre/post snapshots for every real attempt.
- [x] 6.8 Bound `world.inspect.capabilities` to task-filtered invocation facts and capped refs so large tool outputs cannot overflow the agent context.
- [x] 6.9 Seal typed pipeline source directories as canonical self-verifying `openzyme_sealed_source_tree@1` envelopes and verify their internal/tree provenance offline.
- [x] 6.10 Add the strict `fasta_zero_records@1` artifact profile so only derived exact-zero FASTA can represent a healthy empty result and sentinels remain invalid.
- [x] 6.11 Bind formal evidence to exact durable role-scoped delegation/workflow receipts, publish the installed scientific callable/provider/fetch map, and reject approximation or path guessing.
- [x] 6.12 Require explicit bounded MICU context configuration and close the post-drain-response `waiting_approval` visibility race without changing stable failure taxonomy.
- [x] 6.13 Keep failed-coordinator cleanup active through the existing attempt deadline, retry transient cleanup reads/resolves without masking the primary blocker, reconcile selected-session pending approvals in the Web UI without stale overwrite or old-request starvation, and make the Chrome consumer ignore same-type activity projection echoes without a closed decision while treating an explicit rejected command decision as fail closed.
- [x] 6.14 Add strict direct-field artifact response selectors, require attempt-local completed-operation checkpoints after local parser/source errors, and reject duplicate-method or post-failure approvals before external dispatch without weakening the exact formal operation set.
- [x] 6.15 Correct the r14 AOX HMM-capable timeout hierarchy with `s09.exec_policy.v2` (`120s` default/`3600s` finite max), exact pre-dispatch HMMER run-policy validation, and a `7200s` minimum/default formal session bound; record durable async continuation and quiescent sealing separately without implementing it.
- [x] 6.16 Replace the r15 single-`recv(65536)` control-socket assumption with symmetric at-most-one-dispatch-per-connection JSON-RPC 2.0 NDJSON framing, a `4 MiB` request/response payload cap, multi-chunk aggregation, bounded string/int64 request ids with safe/null error identity, observed-trailing-byte rejection and late-second-frame close semantics, malformed/incomplete/oversized fail-closed errors, per-connection worker isolation, SDK preflight/response bounds, and focused regression coverage without changing sandbox protocol/image versions.
- [x] 6.17 Historical pre-r25 step, superseded by 6.23: revise UniProt to `provider_config:uniprot:v2` while retaining one controlled operation/approval, cap the operation at 100000 accessions, partition fixed queries at 100, preserve `batch_size` as page size, apply the Link page cap per query, pin pagination links to the safe UniProt HTTPS search endpoint with digest-only failure, expose the then-gapped default 37722-to-378 resource prediction and query/page transcript, bind every response page to its producing query slice, reject cross-query identity swaps, and replace quadratic duplicate scanning without adopting async ID Mapping.
- [x] 6.18 Narrow the AOX public scanner to the exact four logical provider-manifest suffixes and real sealed-Python `Path(...) / attribute` syntax while continuing to reject unknown absolute paths, traversal, and lookalike text.
- [x] 6.19 Correct the r19 local harness boundary: define provider/register/fetch artifact selectors as mutually exclusive terminal direct-response selectors with no chaining or synthetic envelope, stop every attempt before further external approval/dispatch after any failed operation-bearing sandbox run, and require Host control-socket registration, provider artifactization, and HPC fetch to bind the current Host-sealed run/operation source snapshot explicitly rather than infer stale command state or trust sandbox input; keep cross-run effect adoption proposal-only.
- [x] 6.20 Correct the r22 artifact-kind boundary without widening the domain enum: validate the exact nine `ArtifactKind` wire values locally in the dependency-free sandbox SDK and again at every Host/raw-control boundary with typed non-retryable `artifact_kind_invalid`; reject explicit invalid or mismatched bio-tool runner kind/format before dispatch instead of extension fallback; pin all 17 AOX normalized deliverables to FASTA=`sequence/fasta`, HMM=`result/hmm`, CSV=`result/csv`, or JSON=`result/json` through `aox_fixed_deliverable_artifact_contract@1` in online copies, cache hits, fault evidence and offline verification; retain the typed zero-FASTA exception only for its three declared roles, and preserve r20-r22 as non-reusable NO-GO evidence.
- [x] 6.21 Add a stable trusted-operator Chrome observation helper that converts exact `aox_browser_observation_capture@1` console/page-target/DevTools-request-response/PNG inputs plus the dynamic terminal handoff into the raw 23-field receipt, rejects error console and path/schema drift, waits with the final target absent through not-before, and publishes only with a mode-`0600` sibling temp, file fsync, atomic no-replace install, and parent-directory fsync without inventing Host acceptance timing or stronger browser provenance.
- [x] 6.22 Correct the r23 long-provider runtime boundary: use a fresh repository connection for every session-lease heartbeat/retry, retry only SQLite `BUSY` / `LOCKED` within the current lease deadline, preserve confirmed fencing as non-retryable `runtime_write_fenced` across sandbox/SDK/Host, and make the probe emit public-safe complete `/workspace/output/provider/...` literals without weakening the source verifier; preserve r23 as non-reusable NO-GO evidence.
- [x] 6.23 Correct the r25 scientific/provider boundary: bump EBI HMMER to `provider_config:ebi_hmmer:v2`, use configured `page_size=1000` consistently while treating terminal poll as status/`nreported` only and materializing from explicit page 1 with stable `page_count`/count closure; bump UniProt to `provider_config:uniprot:v3` / `uniprot_primary_sequence_identity@2` and `aox_sequence_length_join@2` with an exact active-plus-typed-inactive `DELETED|MERGED` partition, deterministic inactive exclusion without replacement follow/fetch, `DEMERGED`/unknown fail-closed, ordered response-digest/aggregate and inactive-record-digest offline closure, safe batch/page HTTP diagnostics, and offline-verifiable reason counts/mappings; persist sandbox provider request/observation/error diagnostics without retry or replay; preserve the 17 deliverables and r25 as permanently non-reusable NO-GO evidence.
- [x] 6.24 Bound `artifact.list` final output to `100000` ASCII-safe canonical-JSON characters and `artifact.get` large-string pages to `12000` characters, emit versioned bounded metadata/record summaries, and return `root_only` parent-container hints when current dotted paths cannot address an arbitrary dictionary key; keep typed key/index path addressing as an indexed proposal-only architecture change.
- [x] 6.25 Require `scientific_checks.sequence_join.uniprot_raw_response_artifact_id` for every positive that reaches UniProt, close it over the same formal operation output, artifact provenance and provider receipt, and offline-recompute ordered raw page/body/header release, sanitized record, active sequence-to-FASTA and inactive non-follow semantics; run the final-code `37772`-accession read-only diagnostic (`32176` active, `5596` inactive = `5594 DELETED + 2 MERGED`, `378` response digests, `2561` length-filtered hits, release `2026_02`) while keeping its ordinary `/tmp` outputs explicitly non-sealed and non-cutover.
- [x] 6.26 Close the post-correction AFA/similarity execution identities and read-only real-data preflight without adopting diagnostic bytes into a live attempt.
  - [x] Pin `hmmer_afa_alignment_canonicalization@1`, raw-ASCII-before-normalization failure semantics, exact mixed-radix tuple equivalence, `biopython_trace_guarded_numpy_gotoh@1` with exact Biopython `1.87` / cutover NumPy `2.4.4`, `numpy_three_state_gap_switch_correction@1`, strict binary64 integer bounds, cgroup-v2/v1-quota-ceiling-aware `16`-worker/`128`-pair/`64`-chunk ordered execution, preselected one-worker serial mode, and no backend/version/parallel fallback in source-focused coverage.
  - [x] Run the final-source real HMMER AFA preflight and record `2562` aligned records, `517` total passes including AAB, and `516` non-reference passes as ordinary non-cutover diagnostic evidence.
  - [x] Preserve the superseded pure-v3 receipt `sha256:caf483bedbe2865cdf3be0677dbcb3a27d6ccfb9fd1a57bbc0093a35ef90bcf5` and temporary 2-CPU Podman calibration receipt `sha256:b9749e6c3f23dd553a1e33b55f7cb9a67a1aee6dfbfae8fb4235ce0aa52f563c` as non-cutover diagnostics; do not reuse their old identities or ordinary `/tmp` bytes.
  - [x] Complete final comparison receipt `sha256:ace8baa8bfa070a621186d7b3db3acddcdf39abe26070e72270fc727b0017b5e`: two independent authoritative-source exact-cutover-NumPy-`2.4.4` 2-worker real-candidate runs have identical raw outputs and pin-only-normalized equality to frozen pure-v3 bytes, record unavailable correction activation without inventing zero, distinguish reference-validation NumPy `2.4.6` without claiming a direct full-set patch A/B or allowing fallback, and repin the knowledge workflow plus focused expected hashes in dependency order.
- [x] 6.27 Preserve r27 as permanent NO-GO, implement the bounded canonical artifact-registration metadata transport (`256 KiB` inline / `32 MiB` sidecar), fd-anchored strict Host validation, bounded `artifact_registration_response@2` metadata/validation summaries, strict selector, honest provisional compat-runner response, register-many item/aggregate/prevalidation bounds, real >4 MiB local/compat socket regression, and the exact r27 17,016,803-byte file-backed-SQLite replay without changing the 4 MiB frame or truncating catalog metadata.
- [x] 6.28 Preserve r28 as permanent NO-GO; accept bounded safe product task ids in `world.inspect`, expose the direct-argv/no-implicit-shell contract to agents, reject an unwrapped Python heredoc before source snapshot/run/process creation with a typed corrective hint, and require the next live pin to use a 300-second timeout, one pre-response retry, and configured `max_tokens=8192` request cap without relaxing scientific or nonzero-run fail-closed semantics.
- [x] 6.29 Preserve r29 as permanent NO-GO; propagate safe adapter `error_code/stage/retryable/hint/details.runner_failure` through the sandbox control response and Pipeline SDK without private diagnostic leakage, truthiness coercion, hidden replay, approval reopening, backend fallback, or effect adoption; require a fresh clean commit/SDK pin and fresh roots for the next campaign.
- [x] 6.30 Preserve r30 as permanent NO-GO and record its bounded correction requirement: keep the complete active/inactive identity partition in separate canonical `metadata.json`, replace only the linear FASTA active-sequence map with an exact count/digest/contract summary while retaining fixed provenance, require exact non-bool integer `batch_size`, preflight path conflicts across all artifact drafts before any registration, and require a fresh clean commit/config/SDK pin with fresh roots without raising metadata limits, replaying provider calls, or adopting r30 effects.
- [x] 6.31 Preserve r31 as permanent NO-GO; expose that every otherwise-valid `sandbox.exec` reaching source preflight, including `python -c`, package/signature inspection and diagnostics, requires an eligible non-empty `/workspace/src` and snapshots the whole tree before `SandboxRun`/process creation while earlier validation retains its own errors; keep direct `artifacts.snapshot_code` recovery selection-aware; direct agents to controlled docs or a source-bearing inspection without placeholder source, hidden fallback or provenance weakening; repin changed workflow knowledge and require a fresh clean commit/config/workflow pin plus fresh roots without adopting r31 effects.
- [x] 6.32 Preserve r32 as permanent NO-GO; expose the exact Python return types of every pinned scientific result accessor, require exactly-once UTF-8 encoding before bytes-only writers, retain `metadata()` as a structured dict, and fail closed on type/annotation drift without adding coercion or changing calculation implementations; repin the AOX SOP/workflow and require a fresh clean commit/config/workflow pin plus fresh roots without adopting r32 effects.
- [x] 6.33 Preserve r33 as permanent NO-GO; expose `/workspace/input` as a Host-managed read-only mount in the materialize tool descriptor, mandatory artifacts doc, AOX SOP and formal prompt; require `artifacts.materialize()` to create requested targets/parents while caller source uses `/workspace/work` or `/workspace/output` for mutations; keep `EROFS` fail-closed without remount, alternate-path fallback or duplicate operation, repin the AOX workflow, and require fresh roots without adopting r33 effects.
- [x] 6.34 Preserve r34 as permanent NO-GO and retain its first `hpc_staging_failed` probe fact separately from the later operation-budget guard; correctionally fix live cutover launch/runtime/config/evidence to exactly one durable signal per drain so the driver checks operation/task/sandbox terminal state before claiming a queued master wakeup, rejects any multi-signal pin, and cannot create replacement probe tasks or spend further MICU after a failed turn; require a fresh clean commit/config pin and fresh roots without retrying or adopting r34 effects.
- [x] 6.35 Preserve r35, r36, and r38 as permanent NO-GO evidence (and r37 as an incomplete non-evidence launch root); retain r35/r36 transient `remote_layout` SSH staging failures without replay/adoption; record r38's real HMMER/UniProt progress and Chrome approval before the 106 MiB workspace/approval transaction amplification; add a same-row compact pending-approval public projection, keep hot-loop/cleanup coordination off the composite workspace, reuse bounded artifact collection projections throughout workspace/activity/capability reads, and decouple activity-event backfill from full workspace construction while retaining exact metadata in the catalog and requiring a fresh commit/config pin plus fresh roots.
- [x] 6.36 Preserve r45 and r46 as permanent NO-GO evidence; retain r45's same-member/different-order HPC artifact-set projection failure and r46's real 82,719-hit/83-page HMMER plus Chrome approval without replay/adoption; reconstruct a lost durable provider callback only from the same operation/request's digest-verified sealed request/observation transcript, restore the original bounded summary/transcript/validation/warnings in the complete S12 result envelope, impose an 8 MiB control-document bound and the core 256 KiB complete immutable result-envelope bound, and terminal-known fail closed on missing/tampered/schema/route/config/output/artifact identity drift with focused lost-callback and negative tests.
- [x] 6.37 Preserve r47 as permanent NO-GO evidence; retain its real 68,592-candidate HMMER effect, exact sealed artifacts and Chrome approval without replay/adoption; remove the complete `candidate_accessions` list from EBI HMMER inline summary while keeping exact identities authoritative in digest-bound `provider_parsed/parsed_hits.csv`; validate the complete Host result envelope against the core 256 KiB bound before materialization; and terminalize a terminal-known invalid observation as `recovery_failed` without a repeated claim/reconcile event loop.
- [x] 6.38 Close the pre-r48 qualification scheduling gap without weakening the frozen scenario: preserve a coordinator slot's last non-idle `claim_raced` / `not_claimable` diagnostic when later worker kinds are only idle, keep `semantic_progress=false` with no processed-count or immediate-wakeup authority, add owner-focused coverage, and retain the existing registry, selection, budget and cross-layer oracle.
- [x] 6.39 Preserve r48 as permanent NO-GO evidence after its pre-session `aox_durable_operation_ownership_required` failure; replace newly emitted live configuration with closed `aox_blank_world_runtime_config@2`, bind owner policy/durable route allowlist/command drain/generic mutation closure/bounded shadow observation into `config_digest`, reject ineligible reliability before pin runner attestation or run-live root creation, and retain `@1` solely for frozen offline evidence verification.
- [x] 6.40 Preserve r49 as permanent NO-GO after exact same-content durable-event replay left a standalone SQLite implicit transaction open, caused event-outbox writer retirement to fail with nested `BEGIN`, and prevented attempt quiescence; close the replay transaction through the existing commit/UoW boundary, prove event replay plus mutation-writer terminal retirement on one connection, and require fresh admission/pin/roots without adopting r49 state or effects.
- [x] 6.41 Preserve r50 as permanent NO-GO after a real Chrome-approved EBI HMMER job returned Celery `RETRY` and the adapter misclassified that accepted-job nonterminal state as a non-retryable invalid request; bind `provider_config:ebi_hmmer:v3`, continue polling only the same job id through `RETRY`, extend the bounded poll deadline from the provider's exact 1800-second first-retry boundary to `3300s < sandbox 3600s`, retain timeout/failure/unknown-state fail-closed behavior, and require fresh admission/pin/roots without replaying or adopting r50 effects.
- [x] 6.42 Preserve r50's independently observed probe-attestation failure: closed-validate and carry a runner-issued same-shell toolchain runtime identity from terminal raw result into the immutable durable HPC result envelope, strip private/extra fields, terminalize a present malformed or cross-boundary identity without a reconcile loop, never infer a missing identity, and require all successor probe receipts to arise from fresh operations.
- [x] 6.43 Preserve r51 as permanent NO-GO after its stale immutable sandbox image completed every real NCBI/MAFFT/hmmbuild/EBI-HMMER/UniProt/HMMalign/CD-HIT operation but lacked the exact Biopython backend at similarity time; add a pre-effect `aox_sandbox_scientific_backend_probe@1` to initial and inter-attempt identity resolution using a copied mode-normalized digest-matched SDK plus selected immutable no-pull/no-network/read-only bounded container, validate exact Biopython `1.87`/NumPy `2.4.4`/Gotoh/binary64/numeric/canonical receipt behavior, retain no-install/no-Host-import/no-fallback semantics, keep the reproducible dependency manifest proposal-only, and pause after correction commit without starting r52.
- [x] 6.44 Replace history-poisoning AOX admission with explicit fresh attempt authority, complete occurrence dispositions, agent-selected adopted chain, same-attempt cross-run adoption/materialization, Host-finalized closure, and production `aox_blank_world_attempt_bundle@3`; preserve the frozen `@2` verifier and r48-r51 permanent NO-GO evidence without starting r52.
- [x] 6.45 Complete strict non-live verification and record the pre-live implementation checkpoint immediately before any r52 authorization/root/provider/HPC/browser action; retain dirty-worktree admission as an explicit blocker until review and commit.
- [x] 6.46 Preserve r52 as permanent NO-GO after the first positive child completed six real probe operations but failed probe attestation because the current durable HPC result exposes canonical `run_id`, while the formal master independently failed its next provider call because an over-limit fourth tool call had no matching `ToolMessage`; retain the three-call execution cap, trace every requested call, return and persist structured `parallel_tool_call_limit_exceeded`/`no_effect` results for every undispatched overflow call in both master and teammate loops, strictly normalize completed HPC `run_id` and provider `provider_request_id` into evidence `backend_run_id`, reject missing/noncanonical/ambiguous identity fields, and require a fresh correction commit, full admission, pin, exact-three authority plan and roots without replaying or adopting r52 state or effects.
- [x] 6.47 Close the post-r52 early-return gap by settling each provider response as one ordered tool-call batch: persist overflow no-effect observations before any eligible dispatch can return; on approval, runtime suspension, successful terminal action, or boundary-fatal dispatch, give every later never-dispatched eligible call a durable `tool_call_batch_interrupted/no_effect/verify_then_retry` result and give the failing dispatched call a ToolResult preserving its exact failure observation/effect certainty; emit one ordered disposition per call without hidden execution or retry in both master and teammate loops, update stable architecture docs, and prove the approval/terminal/dispatch-failure combinations with focused and mainline non-live gates before any successor live authorization.
- [x] 6.48 Preserve r53 as permanent NO-GO after its independent six-operation probe sealed successfully but the first formal runtime barrier found no exact AOX observer writer on the open pre-attempt scope; bind one root `aox-attempt-driver:*` writer only for each formal barrier snapshot, retire it before Host-finalized pre-attempt-to-attempt scope rollover, keep all other active writers visible to the barrier, add focused rollover/retirement coverage, and require a fresh correction commit, full admission, pin, exact-three authority plan and roots without replaying or adopting r53 state or effects.
- [x] 6.49 Close the post-r53 runtime-barrier call-surface gap: route both full semantic observation and terminal-command attached-writer settlement through one bounded formal observer context; carry exact purpose and attempt authority into drain coordination; forbid direct formal AOX barrier projection; prove real-SQLite terminal settlement, other-writer visibility, observer retirement and subsequent pre-attempt sealing; synchronize stable architecture docs; invalidate every unconsumed successor plan pinned to `6e5ff65`; and stop before any new numbered live action.
- [x] 6.50 Preserve r54 as permanent NO-GO; distinguish operation success from unsealed selection/attempt/report/campaign success; replace the incomplete selected-chain `@1` admission contract with digest-closed `@2` role-to-operation signatures for new attempts; bind `@2` through active runtime config/pins, qualification and bundle verification; expose deterministic readiness and atomic adoption; preserve real scheduler progress across projection failure; terminalize exact max-step signals while leaving master replan explicit; keep all r54 roots/effects/evidence immutable and stop before any new numbered live action.
- [x] 6.51 Preserve r55 as permanent NO-GO after its exact teammate max-step signal, canonical `agent_can_replan` observation and pending source-bound master wakeup were flattened into `runtime_scheduler_batch_failed`; classify only that fully closed handoff as scheduler-batch settlement, keep the signal failed/task nonterminal/new wakeup independent, retain ordinary/master/incomplete failures as failed, add real-SQLite and r55-shaped regressions, synchronize stable docs, and require a fresh correction commit/admission/pin/authority without consuming another live plan.
- [x] 6.52 Replace the post-r55 Host-side repository reconstruction with a Core-owned typed runtime outcome settlement, make every master/teammate max-step a same-command batch barrier even when `max_signals > 1`, keep business task exit orthogonal to scheduler settlement, prove the boundary with a real file-backed two-command `RuntimeCommandWorker` regression, preserve AOX `max_signals=1` only as a campaign identity/observation constraint, and stop before any new live authority or effect.
- [x] 6.53 Preserve r56 as permanent NO-GO after its probe, Chrome approval, six formal operations, 17 normalized deliverables and scientific-attempt closure were followed by a transient zero-open-scope runtime-barrier failure before report/bundle completion; record the exact non-atomic attempt-to-post scope rollover, MICU lower bound and parent-fatal limits; formally split diagnostic live runs from exact-three formal acceptance without weakening GO or reusing any r56 authority, root, effect, artifact, browser receipt, closure or bytes.
- [ ] 6.54 Make Host scientific transition finalization one short atomic write transaction covering attempt-scope seal, immutable closure and unique post-attempt-scope open; add a real file-backed concurrent barrier regression proving readers observe exactly one pre- or post-transition open scope and never a committed zero-scope gap, while genuine missing/ambiguous scopes remain fail closed.
- [ ] 6.55 Implement a schema-disjoint one-slot diagnostic authority/consumption path, separately named diagnostic runner, fresh diagnostic root namespace and append-only diagnostic decision that are always `acceptance_eligible=false`; prevent it from emitting `aox_blank_world_attempt_bundle@3` or entering the campaign reducer, reject every cross-mode plan/root/receipt/artifact reuse, and keep existing `authorize`/`run-live` exact-three acceptance-only.

## 7. Architecture and operator documentation

- [x] 7.1 Synchronize `docs/OpenZyme架构设计.md`, relevant stable `docs/v3/` documents, AOX execution docs, workflow pack docs, live marker semantics, operator commands, and current limitations with the implementation.
- [x] 7.2 Rewrite the S15 session evidence as historical/non-cutover and document the corrected formula, integer boundary, real provider quorum, blank-world proof, fixture boundaries, and local trusted-Host scope.
- [x] 7.3 Audit all implementation-time harness findings, directly test/document only small corrections, and create separate detailed deferred documents for each major architecture adjustment without implementing them.
- [x] 7.4 Record the deferred two-phase transactional attempt-evidence collector, exact artifact-root closure, failure atomicity, recovery, schema migration, and fresh-live acceptance plan in its own architecture proposal without implementing it in this Goal.
- [x] 7.5 Record the deferred Host-authoritative controlled-operation resource-estimate/limit snapshot, approval/config binding, drift handling, migration, and verification plan in its own architecture proposal without implementing it in this Goal.
- [x] 7.6 Record the deferred immutable metadata-manifest binding, true bounded paging, deduplication, GC, migration and offline-verifier closure in its own architecture proposal without implementing it in this Goal.

## 8. Verification and cutover decision

- [x] 8.1 Re-run scoring/provider/harness/execution/campaign focused tests and ruff after the pre-live harness closure, then fix every regression without weakening scientific gates.
- [x] 8.2 Re-run frontend tests/build, default non-live pytest, `./scripts/check-mainline.sh`, and `uv run python -m openzyme_host_api.evals` from a clean configuration boundary.
- [ ] 8.3 Preflight NCBI/PubMed/EBI/UniProt, MICU, image/SDK, HPC runner/toolchain, and Chrome availability while recording exact blocker evidence.
- [ ] 8.3a After 6.54/6.55 and all non-live gates pass on one fresh clean commit, obtain separate operator approval for one diagnostic authority, run one diagnostic positive-shaped path to a fully settled diagnostic receipt, and keep every diagnostic effect/byte outside the later acceptance plan and reducer.
- [ ] 8.4 Run positive attempt 1 from clean roots and require published report plus passed offline verification.
- [ ] 8.5 Run positive attempt 2 from different clean roots on the identical commit/config and require published report plus passed offline verification.
- [ ] 8.6 Use Chrome for at least one canonical approval and verify same-operation resume, workspace/events/report/evidence consistency, and a clean browser console.
- [ ] 8.7 Run the controlled fail-closed attempt and prove it produces neither a cutover-eligible report nor a valid success bundle while preserving terminal failure evidence.
- [ ] 8.8 Seal the three-attempt campaign decision, report MICU cumulative usage, audit git status/diff and commits, and declare local Live cutover GO only if every criterion passes; otherwise retain precise NO-GO blockers.
