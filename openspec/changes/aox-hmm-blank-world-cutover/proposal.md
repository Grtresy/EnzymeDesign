## Why

OpenZyme V3 已经具备严格的 artifact、sandbox、approval、provider 与 HPC 边界，但当前 AOX/HMM 评测仍缺少产品化的真实位点评分合同、必需文献证据语义和可重复的 blank-world cutover 证明。旧 S15 历史结果与现行 fail-closed doctrine 不一致，因此必须以当前代码、真实外部数据和可离线复核的证据重新建立 GO 基线。

## What Changes

- **BREAKING**：把 AOX 候选筛选中的 `activity_score` 更正为版本化 `aox_motif_rule_score@1` / `motif_rule_score`，明确它是参考位点规则启发式分数，不代表实验活性预测。
- 从只读 reference notebook/runner 提取坐标映射、位点、残基集合、权重和阈值，建立独立实现、源码摘要、golden tests 与严格科学前置错误。
- 建立真实文献证据 quorum：PubMed/PMID/DOI 为 cutover 必需证据，Semantic Scholar/Tavily 为可降级 enrichment；禁止 provider 失败后生成替代证据。
- 建立一次 exact-14 NCBI fetch 到“13 条 HMM model reference + `AAB57849.1` 坐标 reference”的两条显式选择链，再与 EBI HMMER `refprot` → score-filtered UniProt accession → UniProt sequence → identity-preserving length join → scoring-input/HMMalign → motif/CD-HIT/相似度图闭合为端到端身份与 digest 链。
- 建立机器可验证的 blank-world campaign：clean roots、cache bypass、与正式科学 artifact 严格隔离的 known-positive probe、由封存 artifact 重算的 healthy-empty branch/operation omission、无伪造 provider digest 的 skip receipt、sealed evidence bundle 与 tamper verification。
- **BREAKING**：把 motif candidate、conditional-empty 与 normalized final bundle 从 agent-local source snapshot 提升为版本化 exact calculation/finalization capability；Host 在任何 normalized artifact catalog write 之前原子预验证完整 17-deliverable bundle，并签发绑定 session/task/attempt/selection/sandbox source 的 validation receipt。没有 exact passed receipt 时，attempt closure、execution completion 与 report handoff 均 fail closed。
- **BREAKING**：退役已完成但与 current close contract 永久不相容的 closure-stage live diagnostic/authority/reconstruction/CLI 产品链；保留 migration `035`、历史 SQLite rows、封存 evidence 的只读验证和 formal non-adoption gate。
- **BREAKING**：把 sandbox-local pre-admission failure 纳入 canonical typed causal chain：Host 在 operation admission 前封存 `hpc_stage_ref_required|provider_output_path_invalid/no_effect`，terminal run 封存 source-bound `sandbox_exec_nonzero` wrapper，failed ToolResult、`ENGINE_COMPLETED` wake 与 offline formal evidence 复用同一 cause；不由 SDK 裸异常或 automatic observer 覆盖 earliest typed failure。
- 修正 c001 再次暴露的 artifact-catalog ref / HPC stage ref authoring 歧义：selector 继续返回既有 `artifact_id` / `content_digest`，但 SDK 提示与 pinned AOX workflow 明确要求 downstream `bio_tools.*` 输入先经 `workspace.stage_artifact(...)`，禁止 digest 改名或手写 stage descriptor；同步 workflow digest 与 current conductor `@4` 跨来源回归，不改变 Host validator、字段、错误码、I/O、authority、effect 或 retry 语义。
- **BREAKING**：删除 AOX runtime observer、Core runtime-barrier/observer-writer，以及 `run-live` / `run-diagnostic-live` 内置的 drive-until-terminal、no-wakeup 与 scope-rollover policy chain。Codex 测试员只经 public Host API/CLI 编排 message、drain、approval 与只读检查；Host 继续独占 canonical state、approval、fencing、typed effect 与隔离边界，进程监督只证明本地进程退休，offline bundle verifier 与 campaign reducer 是唯一 GO 权威。
- **BREAKING**：删除失去 production caller 的 generic `AttemptRunner`、legacy `@2` emitter、test-only production evidence builder、陈旧 driver contract 与 browser helper；以 exact authority-bound preflight、无编排策略的 supervised Host、可封存 public CLI receipt chain、closed-attempt evidence public export 和单一 source-bound `aox_blank_world_attempt_bundle@3` finalize-and-seal 路径补齐 Codex conductor 的 production reachability。r68 authority 已消费但没有 root/session/attempt，只记录为 prelaunch blocked，不改写为 canonical NO-GO。
- **BREAKING**：post-r68 public receipt 不再伪装 Codex 调用了 agent-owned scientific mutation 或 Host finalizer；这些转换只由 canonical control/events/export 证明。每个 formal ordinal 在 root 前 atomic one-use claim；`aox_closed_attempt_evidence@2` 闭合 exact 三任务、source-linked report、final answer 或完整 `aox_fault_negative_state_closure@1`，第三槽由 authority-bound exact `AOX_ref21.fasta` byte-flip public capability触发；reducer 强制同一 campaign/plan、exact 1/2/3 与全部 attempt identities唯一，production qualification 使用真实 API/file-SQLite composition而非 source inspection。
- **BREAKING**：post-r69 launch plan、slot claim、preflight 与 Host supervision 不再预造 `attempt_id`、`lane_id` 或 admission idempotency shadow truth。current `@2/@3` launch schemas 只绑定 campaign、ordinal、session、task、envelope/root 与 deterministic `launch_id`；executor 先经 canonical lane tools 创建并绑定真实 lane，再由该 task 的 current assignee 调用极小 `attempt.create(envelope_id,idempotency_key)`。Host 从 authority/task/lane/workflow registry 推导 exact admission，并在 assignee recheck 后 late-bind canonical attempt identity。删除 diagnostic authority mint/consume、public scientific mutation/finalizer API/CLI、dead Core `create_attempt` compatibility 与 private admission argument builder；inspect/export/bundle/verifier/reducer 只消费真实 control identities，legacy SQLite/schema/evidence 仅保持只读兼容。r69 在 attempt 创建前因 `attempt_lane_scope_invalid` 停止，只封存为 consumed pre-admission blocked，非 canonical NO-GO。
- **BREAKING**：post-r70 formal plan/claim/preflight/supervision/bundle 进一步删除 speculative task-bound identity、预生成 authority envelope/request 与 hidden exact `task.create` matcher。current `@3/@4` launch schemas只约束 consumed campaign/ordinal/session/root/policy；Codex conductor 必须先经 public message、bounded drain、sealed terminal status 与 canonical workspace 唯一读取真实 execution task，随后才把 operator authority 原子 late-bind 到它。每个 drain 同时封存 admission/terminal response，并要求 terminal response exact 匹配唯一 `runtime.command.finished` event；digest-only status GET、未封存 handoff 与 event drift 均非 terminal proof。CLI 对 sanitized non-2xx 使用同一 bounded sealing 语义；production qualification 由真实 public FastAPI client、file SQLite 和 deterministic model/runtime composition 证明，不允许 private service、手工 `ToolRegistry` 或 synthetic receipt 自证。r70 只封存为 authority/slot/root/session/receipt 已消费、runtime drain 未提交且 scientific authorization/admission/attempt 未创建的 pre-runtime conductor blocked，非 canonical NO-GO；全部 r70 state 不可复用且本 repair 不创建 r71。
- **BREAKING**：post-r71 supervised Host 在 listener、session、model/provider 与任何 scientific mutation 之前，必须以同一个 injected sandbox runner 完成一次 exact authority/preflight-bound runtime identity bootstrap；fresh SQLite 的 image/session/workspace registry 必须全空，随后只允许原子写入并重读一个 immutable cutover-grade image record，并把 source-bound bootstrap receipt 封入 child-ready、startup 与最终 bundle。删除 dev/eval 的 ambient image registration；缺失、malformed、image/SDK/runtime drift、任意 preexisting row、写入/重读失败全部 fail closed，且不得 pull/build/install/tag/fallback。r71 已消费 authority/positive slot/root/session/tasks/authorization/receipt 与 `436,249` MICU，但在 `sandbox_image_missing` 后、scientific admission/attempt/provider/HPC 前停止，只封存为 pre-attempt sandbox-bootstrap blocked，非 canonical NO-GO；全部 r71 state 不可复用。
- **BREAKING**：post-r72 qualification runner 在任何 collection/harness/scenario 前统一验证 checkout 外 canonical no-replace output，并以 canonical-checkout-bound、kernel-held、nonblocking single-flight 拒绝同一 checkout 跨 mode/output 并发复入；分别返回 `architecture_qualification_output_invalid` 与 `architecture_qualification_run_active`。Codex conductor 对 yielded cell/session 只能恢复原 handle，失联后只能只读停止，禁止 automatic admission recovery、等价命令 relaunch 或把 recheck/recovery report 采纳为 fresh admission。r72 的重复 full qualification、focused recheck、invalid-parent recovery 与 stop state 只封存为 prelive conductor blocked，非 canonical NO-GO，且全部不可复用。
- **BREAKING**：post-r73 删除 conductor-owned `started_head`/drift/recovery/adoption truth 和 terminal failure 后的 equivalent relaunch；r73 因 stale conductor HEAD shadow truth 丢弃首份 `789f1c1` report、串行重复 full admission，且 harness timeout 只留下 digest/cascade，故只封存为 prelive conductor/qualification blocked，非 canonical NO-GO，所有 report/reproduction/stop state及旧 persistent goal不可复用。qualification runner从Host production package删除并合并到repository test-gate单一bounded executor；该阶段 report/receipt升级为source/run-causal-bound `@2`，随后策略中性变更以 owner/constraint 与 transformation proof 将 current report/receipt升级为`@3`，历史`@1/@2`只读且不得adopt。
- **BREAKING**：上一阶段将 `openzyme-aox-cutover` 启动失败升级为 `aox_cutover_launch_failure@2`，只允许错误源明确标记的字段级原因进入 `failure_details`；内部详情、配置值、路径、凭据与异常链仍不公开。删除未被产品路径消费的 `ResearchSettings.mcp_enabled` 环境影子真值，AOX 的 MCP 可用性继续由 Host 权威配置明确投影。Codex 测试操作员必须区分封存观测、源码推论与未证实假设，后者不得成为纠正后重试（corrected retry）或授权消费（authority consumption）的依据。
- **BREAKING**：新增无外部副作用的公开 `openzyme-aox-cutover check-config`，以 production 设置解析器和 AOX 闭集 normalizer 生成 `aox_cutover_config_check@1`，删除 Codex conductor 直接 import 私有 settings/builder 自证配置闭合的路径。`pin` 仍是首次真实 forced-SSH fixture attestation；其失败升级为 `aox_cutover_launch_failure@3`，以闭合 tagged union 保留 schema 字段原因，或 source-bound runner `tool_id`、run/receipt identity、stage、effect certainty 与可安全公开的 `runner_error_code`。历史 `@1/@2` 仅只读，命令执行次数与 goal 阻塞审计次数必须分开报告。
- **BREAKING**：把 full qualification 所需的本地 `socketpair`/跨线程事件循环唤醒明确纳入 Codex conductor 执行环境合同。获准的一次 current qualification 必须对唯一公开 repository script 第一次且仅一次使用窄范围 sandbox 外权限；禁止先在普通 sandbox 试跑、替换 uv cache、raw pytest/IPC 探针、改换 output 或 terminal 后等价重发。权限在进程启动前不可得时以零次执行的 operator-environment blocker 停止，不伪造 qualification report；`check-config` 与 forced-SSH `pin` 继续保持独立权限和 effect 边界。
- **BREAKING**：修正 f778aa1 ledger 与 ec9151b check-config 暴露的 launcher/product 计数混淆：current source 证明只读、零 external effect 的 `check-config` 与无 `--output` ledger，在 ordinary sandbox 明确 platform rejection、无产品结果/effect 后允许一次 exact permission-only recovery，并分开记录 launcher/escalation/adoptable-result；typed result、unknown effect、输入或 launcher 变化、非只读命令及第二次升级均不得用该 permission-only rule 重发同一 occurrence，后续只按 current effect/identity/lifecycle/authority/budget gate 显式选择。public owner 与 `uv`/`.venv` launcher 分离；qualification、`pin`、`preflight` 继续首次 issuance 前提权。历史 preparation 均冻结且不追溯复活。
- **BREAKING**：把 R-series blanket stop 收窄为 exact occurrence continuation。新增 machine-readable `aox_config_contract@1`、credential-free atomic `aox_config_candidate@1` 与 `aox_cutover_config_check@2`，使 typed config failure 只拒绝 exact candidate，agent 可显式发布 identity-distinct candidate 后再次验证。pin 改为 deterministic reserve-first，duplicate submit 只观察，并通过 public exact handle query/resume/reconcile 处理同一 occurrence。authority consumption 对同一 exact plan/request 收敛到 durable receipt；formal conductor contract 升级为 `@4`，public Host receipt 升级为 `@3` 并绑定 caller-selected request identity、effect/retry/reconciliation 与 occurrence-local terminal scope。新 occurrence 只在 `no_effect + disposition` 或 completed exact reconciliation 后按 current authority/budget 显式创建；禁止 auto-fill/retry/loop/replacement dispatch/策略选择，保持 exact-three、每 slot `max_attempts=1`、preflight canonical NO-GO 与 preflight failure schema 不变。
- **BREAKING**：补齐 `config-contract` 的可执行 profile discoverability：删除 candidate 自有的 `OPENZYME_*` prefix/name allowlist 第二真值，改由 production settings/reliability resolver 实际消费 canonical field descriptors，并把 env aliases、value kind、安全通用默认值、normalization/identity projection 与 runtime-normalizer 同源 AOX eligibility 公开投影；缺字段或 constraint projection drift fail closed。candidate 只绑定 listed relevant sources，unlisted environment 忽略，credential 仅按 presence、private non-credential 按 digest、ledger/runner path 按 path/content identity 投影；同 presence 的 credential value 变化不再伪造 identity-distinct occurrence。同步 active spec、主架构、稳定 V3、owner/qualification registries 与 R-series skills，保持 candidate lifecycle/effect、`check-config`、pin/authority/live 边界不变。
- **BREAKING**：c002 证明 `aox_cutover_launch_failure@3.failure_details` 把 candidate/runner occurrence 与 schema/sandbox/runner cause 放进互斥单槽，`check-config` wrapper 因而以 lifecycle 覆盖 runtime normalizer 已授权的字段 cause。current writer 升级为 `aox_cutover_launch_failure@4`，由唯一 AOX-family normalizer 独立投影 `failure_occurrence + failure_cause`；`check-config` 同时保留 exact candidate occurrence 与 credential-safe schema cause，runner 同时保留 operation occurrence 与 safe machine cause。触发式升级 formal preflight 为 `aox_formal_preflight_failure@2 / actual_launch_guard_pre_slot_claim`，历史 launch `@1/@2/@3` 与 preflight `@1` 严格只读；OpenZyme 读取 protected environment，Codex 只消费 public candidate/check，不得读取 env、auto-fill、retry/loop 或 secret projection。
- **BREAKING**：r75 暴露 pin/authority 与 preflight/Host 之间的 command-scoped launch profile 丢失：authority 已消费，但 preflight 从 ambient environment 回落到 `legacy_only_v1`，在 slot claim/root/Host/session/attempt/effect 前停止，且当时没有 current closure schema，因此保持 blocked/noncanonical。前向 `pin` 原子增加 credential-free `aox_cutover_launch_profile@1`，current authority plan/consumption/preflight 与 Host 只复用其非敏感配置，ambient 只补 credentials；同型 pre-claim 配置失败封存 source-bound `aox_formal_preflight_failure@1` 并由专用 offline decision 形成 canonical NO-GO，不追溯回填 r75 或伪造 attempt identity。
- **BREAKING**：r76 已消费 slot claim/root，却在 Host child-ready 前因 `host_sandbox_runtime_identity_missing` 停止；旧 preflight 只比较 effective config，未执行 actual launch resolver，旧 child failure frame又缺少可离线重建的进程与零状态 receipt，因此保持 blocked/noncanonical。前向 preflight 在 claim 前重跑 actual Podman/image/SDK/`aox_sandbox_scientific_backend_probe@2` guard；claim 后仍发生的 safe sandbox-bootstrap failure 只有在父进程验证 live PID/PGID/start-time、完整退休 process group、fresh root/zero SQLite/effect 与 unchanged MICU 后，才可封存 `aox_supervised_host_pre_ready_failure@1` 并进入 discriminated `aox_formal_slot_failure@2 / closure_mode=pre_child_ready`。历史 `@1` 只读，不回填或复用 r76。
- **BREAKING**：修复 r82 暴露的 preflight-to-spawn TOCTOU：parent 在 `process.start()` 前必须对 exact clean root 与 pinned MICU baseline 原子 no-replace 发布唯一 attempt-start claim，并立即重读 ledger；child、startup/supervision、formal failure 与 public finalizer 全链绑定同一 claim/epoch，current finalizer 不再接受 caller-supplied standalone `micu-before`。无可验证 PID 的 spawn failure 只封存最小 typed blocker，保持 effect unproven、retry terminal 和 successor blocked，不伪造 Host closure 或 reducer decision；历史 schema 只读且不得驱动新执行，frozen r82 不回填、不重试。
- r83 的sealed payload仍为`claimed`，含`terminal`的caller locator不构成terminal proof；该轮保持`operator_false_stop / potentially_recoverable_but_unverified`、blocked/noncanonical且不访问/恢复。前向合同让receipt route与typed payload拥有handoff语义，并以薄skill、CLI help、`0..N` fixture与Event production composition防止同类误读，不改变产品runtime。
- 修复 r79 暴露的 runtime-command late-scope heartbeat authority 缺口：command 在无 mutation scope 时开始、同一 bounded batch 打开 attempt scope 后，每次后续 lease heartbeat 必须使用绑定 exact command id 的短 `runtime_command` writer并立即退休；repository将精确SQLite mutation-guard rejection翻译为包内类型化异常，worker不得解析原始错误文本，只对该authority-transition类型与SQLite `BUSY/LOCKED`在当前lease内有界重试，token/fence/state漂移继续fail closed，且不得replay scheduler。production-composition qualification以真实续租成功事件作为确定性屏障，不依赖固定亚秒sleep。r79的slot 1、open attempt、缺失selection与failure evidence保持blocked/noncanonical，不回填、不重试，也不启动slot 2/3。
- **BREAKING**：在 r56 暴露首个 eligible result 前的 framework defect 后，将单 positive、永久 non-cutover 的 diagnostic live run 与 exact-three formal acceptance campaign 拆成 schema/authority/root/evidence 互斥的两类；diagnostic 不生成 `@3` bundle、不进入 reducer，也不降低或替代正式 GO 门槛。
- 以同一 commit/config 下两次独立正向 live E2E 和一次故障注入作为 local Live cutover GO 门槛，并同步修正旧 S15 历史结论、UI/approval 验收与稳定架构文档。

## Capabilities

### New Capabilities

- `aox-motif-scoring`: 版本化 AOX motif rule scoring、坐标映射、golden reference 与科学 fail-closed 合同。
- `scientific-evidence-quorum`: 必需文献/provider 证据、可降级 enrichment、数据身份与 provenance 语义。
- `blank-world-live-cutover`: clean-root live campaign、known-positive/empty-result、sealed evidence、离线复核和 GO 判定。

### Modified Capabilities

无。现有 OpenSpec 主规格只覆盖 MCP knowledge 与 HPC runner；本变更新增 V3 科学产品能力规格，不修改其既有 requirement。

## Impact

- 影响 `packages/openzyme-{research,runtime,core,engines,pipeline,tools,execution}` 的科学合同、provider adapter、执行与 artifact evidence。
- 影响 `apps/openzyme-host-api` 的 foundation、eval/live gate、统一 AOX final validator/finalizer、Host typed failure、证据聚合、closed-attempt public export、policy-free Host supervision、API projection，以及 public CLI 的 approval/report/evidence receipt 呈现；删除 closure-stage runnable CLI surface、AOX runtime observer、automatic live driver、browser helper 与 one-shot AOX recovery path。
- 影响 AOX/HMM workflow pack、S15 文档、主架构文档、live pytest markers 与 policy-free authority/evidence 命令。
- 影响 repository qualification runner 的 pre-work output admission、checkout-level single-flight 和 Codex yielded-handle 操作合同；不新增 product state、observer、retry queue 或 admission recovery。
- 影响 validation skill、operator goal、AOX 稳定文档与职责化跨来源静态回归中的 preparation effect-class/platform-recovery 合同；不修改 public CLI implementation、SQLite ledger owner、launcher 或平台 runtime。
- 影响 AOX executable settings descriptors、config contract/candidate、toolchain-pin continuation、authority consumption、thin Host receipt、formal conductor execution contract 与 owner/invariant registry；新增 public exact-occurrence/config-field facts，不新增 automatic driver、AOX retry FSM、科学策略或 campaign terminal owner。
- 影响 `packages/openzyme-core` 的 durable runtime-command heartbeat authority，以及真实 FastAPI/thin CLI/file-SQLite production-composition qualification；不修改 public schema、reclaimer 或 scientific terminal semantics。
- 影响 `apps/openzyme-host-api` 的 attempt-start、Host supervision evidence、formal failure 与 public bundle schema；新增 attempt-scoped claim 和 no-PID blocker，但不新增 campaign-global lock、automatic retry 或 reducer owner。
- 真实运行继续依赖现有 MICU 500M 持久账本（历史 usage 不重置）、NCBI identity、UniProt/EBI/PubMed 网络能力、可信 Host-only HPC runner 和 immutable sandbox/toolchain identity。
