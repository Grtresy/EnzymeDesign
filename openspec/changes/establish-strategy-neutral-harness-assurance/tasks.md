## 1. Boundary inventory and red controls

- [x] 1.1 Add the closed owner/constraint registry with identity, owner, lifecycle, persistence, compatibility, error/effect, consumer, forbidden-edge, and scenario fields.
- [x] 1.2 Add registry validation and static architecture tests for unique owners, live source references, generic-Harness/AOX separation, qualification/product dependency direction, and non-authoritative telemetry.
- [x] 1.3 Add controlled red-oracle cases for strategy phase veto, earliest-cause overwrite, automatic recovery work, and private/synthetic positive reachability.

## 2. Delete the generic strategy-policy path

- [x] 2.1 Move source-linked AOX report evaluation to the public product-closure owner without changing its pure canonical-state semantics.
- [x] 2.2 Delete `AoxFinalizationToolPrecondition` and remove `tool_dispatch_precondition` from Host dependencies/service, Harness context/router, master runtime, and delegated teammate propagation.
- [x] 2.3 Replace policy-script tests with owner-local safety negatives plus early-report-delegation, reordered action, ordinary-failure, and honest-ineligible-final-state regressions.
- [x] 2.4 Confirm authority, assignment, active scientific lifecycle, quiescence, fencing, unknown-effect, provenance, isolation, and atomicity guards remain at their owning domain boundaries.

## 3. Remove conductor shadow truth

- [x] 3.1 Emit current `aox_blank_world_runtime_config@5` without a conductor/driver policy object and retain explicit read-only `@1`-`@4` normalization.
- [x] 3.2 Update launch, evidence, fixtures, validators, and offline compatibility so only exact `@5` is current-admissible and historical configs are never silently promoted.
- [x] 3.3 Replace the r-series Codex goal with a compact public-only source-discovering operator protocol and move historical status to cutover documentation.

## 4. Strategy-neutral and world-fidelity qualification

- [x] 4.1 Add `hypothesis` as a dev-only dependency and bounded Core property tests for ordinary trace transformations.
- [x] 4.2 Add a deterministic public production-composition strategy-neutrality scenario covering order permutations, inserted reads/prose, safe rejection follow-ups, early reporting delegation, and bounded turns.
- [x] 4.3 Add a source-bound world-fidelity scenario covering typed cause/wrapper delivery, no history poisoning, public visibility before the next decision, and no synthetic fallback.
- [x] 4.4 Upgrade the invariant registry to `@2`, qualification report to `@3`, AOX receipt to `@3`, and bind owner-registry plus transformation-result identity into current verification/admission.
- [x] 4.5 Register the new families in premerge/full selection, update P0/manifest/resource closure, and prove scripted reachability alone cannot satisfy current admission.

## 5. Stable documentation and contracts

- [x] 5.1 Synchronize `docs/OpenZyme架构设计.md`, Harness Doctrine, target/control/runtime/LLM-loop/failure docs, architecture qualification README, and AOX cutover docs with the owner-versus-strategy boundary.
- [x] 5.2 Remove stale precondition/version claims and document historical schema/report/config compatibility plus forbidden fallbacks.
- [x] 5.3 Strict-validate the new change and the active AOX/qualification changes; update workflow/resource digests required by changed stable docs.

## 6. Non-live verification and handoff

- [x] 6.1 Run focused Core, Host API, AOX config/evidence, architecture scenario, registry/report/runner, and static-boundary tests.
- [x] 6.2 Run Ruff, V3 eval, strict OpenSpec validation, and `./scripts/check-mainline.sh` without live markers.
- [x] 6.3 Run one full diagnostic qualification against the exact current source, verify it is non-adoptable, and do not start rNN/live/MICU/provider/HPC/Chrome.
- [x] 6.4 Audit production net deletion, worktree diff, compatibility readers, and remaining task/spec coverage before handoff.
