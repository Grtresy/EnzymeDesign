## Why

当前 `aox-hmm-blank-world-cutover` 仍以 artifact catalog、`HpcStageRef` 和 17 件 artifact bundle 为正式合同，与已决定的文件化 workspace breaking refactor 不可并行成为当前真相。应先把 c001 与尚未执行的 8.3–8.8 明确裁决为 legacy NO-GO，并阻止任何旧合同上的后续 live 行为。

## What Changes

- **BREAKING**：正式 supersede 当前 AOX/HMM cutover，冻结 c001、既有 receipts、authority、bytes 和六项未执行 live tasks；它们不得恢复、补跑或被解释为新架构证据。
- 记录机器可读的 supersession decision，明确旧 change 不向 main specs 合并 artifact/staging 合同。
- 在不启动 provider、HPC、MICU、Chrome 或新 scientific attempt 的前提下，固定 legacy NO-GO 的只读迁移边界。
- 要求文件化架构完成后通过新的 OpenSpec、fresh pin 和 fresh authorization 重新建立 AOX cutover，禁止把历史 artifact 迁移结果直接 adoption 为新证据。

## Capabilities

### New Capabilities
- `aox-artifact-cutover-supersession`: 定义旧 AOX campaign 的冻结、不可恢复、不可采纳与后继 cutover admission 合同。

### Modified Capabilities

## Impact

影响活动 OpenSpec `aox-hmm-blank-world-cutover`、AOX 稳定文档、campaign/operator runbook 与后续历史 artifact 迁移。该 change 不修改应用代码，也不授权任何 live 行为。
