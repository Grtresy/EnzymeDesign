## 1. Baseline and supersession

- [x] 1.1 Inventory every active turn-recovery obligation, settlement, hypothesis, disposition, synthetic wakeup, assistant-response precondition, lifecycle/readiness duplicate, and Lane consumer.
- [x] 1.2 Mark `make-turn-recovery-semantically-complete` as superseded and prohibit its remaining real MICU task without deleting historical evidence.
- [x] 1.3 Add or rewrite baseline regressions so ordinary no-effect/proven-terminal failures cannot mutate task, approval, scientific, or external-operation state.

## 2. Minimal Harness turn boundary

- [x] 2.1 Delete `AgentTurnRecoveryObligation`, settlement kinds/matchers, response rejection, unresolved fatal error, context state, events, and all Harness exit gates.
- [x] 2.2 Preserve ordinary ToolResult continuation and existing `COMPLETED`, `WAITING_APPROVAL`, `MAX_STEPS_EXCEEDED`, and genuine boundary-fatal behavior without adding a replacement status or phase.
- [x] 2.3 Replace known-trace exact-settlement tests with prose/read/corrected-retry/unrelated-success/multi-failure/step-bound invariants and retain unknown-effect/authority/fencing negative controls.

## 3. Remove the duplicate failure recovery control plane

- [x] 3.1 Remove active `failure.hypothesis.record` and `failure.recovery.record` tools, catalog entries, domain/repository exports, projections, and runtime consumers while retaining historical migration compatibility.
- [x] 3.2 Delete failure-disposition reconciliation, scheduler hooks, `RECOVERY_REQUIRED` signal semantics, prompt reconstruction, and continuation-delivery allowances.
- [x] 3.3 Remove obsolete repository/migration behavior tests and prove existing databases with historical tables still open without producing new hypothesis/disposition records.

## 4. Remove strategy policy from the runtime path

- [x] 4.1 Delete `assistant_response_precondition` from Harness context, master/teammate drivers, scheduler/runtime propagation, Host composition, and API tests.
- [x] 4.2 Remove AOX report-handoff/close-order narration veto and its prompt instructions while retaining mutation-time authority/domain validation and final verifier rejection.
- [x] 4.3 Make AOX supervision, runtime observation, evidence, and closure-stage consumers use the canonical derived lifecycle/readiness and preserve the earliest typed causal error.

## 5. Lane and runtime complexity audit

- [x] 5.1 Prove which Lane fields/tools/API paths still own real workspace/worktree isolation and remove every Lane equality/copy that existed only for deleted recovery proof.
- [x] 5.2 Record an evidence-based keep/demote/delete decision for Lane and the single-process session lease; do not retain either solely because it appears in the doctrine.

## 6. Documentation and validation

- [x] 6.1 Synchronize `docs/OpenZyme架构设计.md`, relevant `docs/v3/` contracts, complexity audit, and AOX status without rewriting historical run verdicts.
- [x] 6.2 Run focused Core/Host/AOX regressions, strict OpenSpec validation, ruff, non-live eval and repository mainline checks; no live marker or external MICU/provider/HPC/Chrome action.
- [x] 6.3 Confirm production code is net deleted, inspect the complete diff and cleanly create one local Conventional Commit.
